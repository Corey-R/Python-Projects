


def print_app():
    name = (__name__) # Dunder that gets the name of the file "app.py" and stores it in name variable
    return name  # returns for display the contents in the variale "name"

"""
    NOTE: Nothing happens if you were to run this because the function hasn't actually
    been called. You must do so by performing the following syntax below.
"""

##print_app() # calls the print_app function which will return the "name" variable
